import Follow from '../models/follow.model.js';

export const createFollow = async (req, res) => {
    // Cria um novo Follow no banco de dados
};

export const getFollows = async (req, res) => {
    // Busca todos os Follows no banco de dados
};