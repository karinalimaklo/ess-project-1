import Report from '../models/report.model.js';

export const createReport = async (req, res) => {
    // Cria um novo Report no banco de dados
};

export const getReport = async (req, res) => {
    // Busca todos os Report no banco de dados
};